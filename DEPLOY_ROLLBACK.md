# Откат деплоя (Railway / poputka88)

## До HTML-ответа в цепочке (In-Reply-To + спуф тема/имя)

| | |
|---|---|
| **Коммит** | `dc41cba7ab7dbec39d3525cee1ffeec82e4908b5` |
| **Короткий** | `dc41cba` |
| **Сообщение** | Notify Message blocked DSN only after HTML sends. |

### Вернуть код на Railway

```bash
git checkout dc41cba
git push origin HEAD --force
```

(или в Railway: Deploy → выбрать деплой с коммитом `dc41cba`)

### После внедрения threading

Запишите здесь новый «безопасный» коммит после успешного деплоя:

- **Коммит threading:** _(заполнить после push)_
